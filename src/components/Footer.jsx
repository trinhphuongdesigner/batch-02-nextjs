import React from 'react';

function Footer(props) {
  return (
    <div style={{
      width: '100%',
      height: '400px',
      background: 'gray',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      Footer of layout 
    </div>
  );
}

export default Footer;